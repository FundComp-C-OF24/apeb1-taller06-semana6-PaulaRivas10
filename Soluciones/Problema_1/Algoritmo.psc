Algoritmo Problema_1
	// Declaracion de variables
	// num, den, i[1-n]
	// i,n,i[1,n]
	i <- 1
	n <- 6
	num <- 1
	den <- 10
	// Bucle para generar la secuencia
	Mientras i<=n Hacer
		Escribir num, '/', den
		num <- num+1
		den <- den+1
		i <- i+1
	FinMientras
FinAlgoritmo
